package com.pokebible.validator;

import javax.validation.groups.Default;

/**
 *
 * Declare OnUpdateGroup used to know the context of validation (Insert or UPDATE)
 *
 */
public interface OnUpdateGroup extends Default {
    
}
